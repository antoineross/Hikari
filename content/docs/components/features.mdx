---
title: Features
description: Setting up Features
---

import { Feature, FeatureHover } from '@/components/docs/features';

## Features
If you want to display the features in a grid, you can use the `Feature` component.

<Feature />

## Features Hover
To add a little bit of a kick to the features, you can use the `FeatureHover` component. Try hovering over the features to see the effect.
<FeatureHover />

